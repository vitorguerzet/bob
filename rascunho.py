import unittest
from matpower_classes import Bus, Generator, Branch, Transformer, GeneratorCost, MatpowerCase
buses=[]
buses.append(float(input('ligma\n')))
print(buses)
buses.append(input('ligma\n'))
print(buses)